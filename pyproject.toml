[project]
name = "csmarketapi-client"
version = "0.1.0"
description = "API client for CSMarketAPI from csInvest"
readme = "README.md"
authors = [{ name = "Luis", email = "luis@softwerk.ai" }]
requires-python = ">=3.13"
dependencies = [
    "dotenv>=0.9.9",
    "httpx>=0.28.1",
    "pydantic>=2.11.7",
    "pytest>=8.4.1",
    "pytest-asyncio>=1.1.0",
]
